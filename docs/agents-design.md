# Respect — детальная спецификация агентов 1–7

Дата: 2026-09-17. Цель документа: до выбора решений по инфраструктурным проблемам — собрать полную картину того, что нужно каждому агенту (вход/выход, где хранится состояние, какие доступы/учётки нужны, на каких механизмах он работает) и какие проблемы это создаёт. Список проблем — в конце.

## Модель данных конвейера (сквозная, нужна до агента 2)

Сейчас в репозитории есть только `data/источники.xlsx` (агент 1). Агентам 2–7 нужно ещё минимум два хранилища:

- **`data/статьи/`** — по одному файлу на статью (markdown + frontmatter), поля: `id`, `source_url`, `title`, `category` (пол/возраст/тема — см. открытый вопрос), `status` (`collected` → `selected` → `approved`/`rejected` → `published`), `image_path`, `published_at`, `telegram_message_id`. Файловый подход выбран по аналогии с источниками — человекочитаемо, диффится в git, не требует БД/сервера.
- **`data/подборки/<YYYY-MM-DD>.md`** — сводка дневной подборки агента 3: список из (до) 5 отобранных статей на этот день, для агентов 4/6 и для просмотра пользователем.
- **`data/аналитика.xlsx` или `data/метрики/`** — снимки метрик канала по дням (подписчики, просмотры, реакции по посту), для агента 7.

Без этой модели агенты 2–4 и 6–7 не смогут передавать друг другу состояние — это блокер уровня «до кода», не деталь реализации.

## Агент 1 — Поиск источников (уже работает)

- Механизм: облачная рутина (`trig_01JZ8oYGe7ceKnuwFBCoEgqu`), ежедневно 9:00 МСК.
- Вход: `data/источники.xlsx` (текущее состояние) + веб-поиск.
- Выход: новые строки в `data/источники.xlsx`.
- Статус: работает, но пушит в отдельную ветку, не в `main` (см. «Проблемы» ниже) — сегодняшний прогон вручную смержен.

## Агент 2 — Сбор и категоризация

- Механизм: вариант А (облачная рутина) — категоризация требует суждения.
- Вход: строки `data/источники.xlsx` с «Подтверждение» = «Да» (только подтверждённые пользователем источники).
- Действие: для каждого подтверждённого источника — обойти его (WebFetch/WebSearch), найти релевантные статьи, которых ещё нет в `data/статьи/` (сверка по `source_url`), сохранить как markdown-файлы со статусом `collected` и присвоенной категорией.
- Выход: новые файлы в `data/статьи/`.
- Возрастные периоды **решены 2026-09-18**: 40–49 / 50–59 / 60–69 / 70–79 / 80+ (см. `docs/audience-behavior-rules.md`, раздел 5). Финальный список тем публикаций пока не зафиксирован — работает по черновой схеме.
- Периодичность: та же ежедневная рутина или сразу вслед за агентом 1 — нужно решить (см. проблемы).

## Агент 3 — Раскладка по хранилищу

- Механизм: вариант Б (детерминированная раскладка, без суждения) — **решено 2026-09-19**: функция отбора 5 статей на публикацию из агента 3 убрана, он больше не выбирает и не оценивает статьи.
- Вход: файлы непосредственно в `data/статьи/` (не во вложенных папках) со статусом `collected`.
- Действие: переместить (`git mv`) каждую прошедшую валидацию статью в `data/статьи/<пол>/<возраст>/<тема>/`, статус `collected` → `sorted`.
- Выход: те же статьи, разложенные по папкам, со статусом `sorted`.
- Зависит от результатов агента 2, то есть должен запускаться позже него в тот же день (или на следующий день, если конвейер суточный) — вопрос очерёдности внутри одного дня ещё не решён.
- **Открыто:** кто и как теперь выбирает статьи на публикацию (бывшая функция «5 на сегодня») — из конвейера этот шаг убран вместе со старой ролью агента 3, а агент 4 по-прежнему описан ниже как работающий со статусом `selected`, которого теперь никто не проставляет (см. проблему в конце документа).

## Агент 4 — Проверка непротиворечивости

- Механизм: вариант А, облачная рутина `trig_01C8nQkEDQYTQxCPq9Pp5LcA`-подобная (см. агент 3), ежедневно 13:00 МСК.
- Вход: статьи со статусом `sorted` (разложенные агентом 3 по `data/статьи/<пол>/<возраст>/<тема>/`, поиск рекурсивный) + архив уже опубликованных (`data/статьи/**` со статусом `published`).
- Действие: сверить факты/рекомендации на противоречия с архивом; если архив пуст (публикаций ещё не было) — одобрять без содержательной сверки, но явно отметить это в резюме, а не молчать. Вердикт `approved`/`rejected` (с причиной в frontmatter, поле `rejection_reason`).
- Выход: обновлённый статус статьи.
- Прямых блокеров нет, кроме зависимости по данным от агентов 2–3.

## Агент 5 — Генерация изображения

- Механизм: **решено 2026-09-17** — не отдельный код с платным image-gen API, а облачная рутина, которая сама пишет простую векторную SVG-иллюстрацию (в духе плоской иконографики) под тему статьи, без внешних ключей. Подключённый MCP-коннектор `visualize` для этого не используется — он рендерит виджеты внутри интерфейса Claude, а не отдаёт файл на диск, поэтому не подходит как есть.
- Вход: статьи со статусом `approved`, у которых ещё нет `image_path`.
- Выход: `data/статьи/images/<id>.svg`, путь сохранён в `image_path`.
- **Новый блокер (см. проблему №9):** Telegram `sendPhoto` не принимает SVG напрямую — нужен растр (PNG/JPEG). Агент 6 пока не умеет это конвертировать.

## Агент 6 — Публикация в Telegram

- Механизм: **решено 2026-09-17** — тоже облачная рутина (не отдельный сервер/код), дергает Telegram Bot API напрямую через `Bash`/`curl` (`sendMessage`/`sendPhoto`). Так проще, чем разворачивать отдельный код без сервера под него.
- Вход: статья `approved` + (если есть подходящий растровый файл) картинка.
- Действие: если `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHANNEL_ID` не заданы в переменных окружения рутины — **не пытается публиковать**, честно фиксирует в резюме «не настроено» (тот же паттерн честного `NotConfigured`, что в коннекторах Bayesum). Если заданы — публикует текстом (`sendMessage`), т.к. картинка сейчас в SVG, не в растре (см. блокер агента 5); статус → `published`, сохранить `telegram_message_id`.
- **Блокеры (ничего из этого пока не существует):** нет Telegram-канала, нет бота/токена, бот не добавлен админом в канал — рутина создана и ждёт эти секреты.

## Агент 7 — Аналитика

- Механизм: **решено 2026-09-17** — облачная рутина, Telegram Bot API (`getChatMemberCount` для подписчиков; просмотры постов Bot API не отдаёт для каналов — только подписчики и, если включены, emoji-реакции через `message_reaction`).
- Вход: `telegram_message_id` опубликованных постов + те же `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHANNEL_ID`.
- Выход: `data/аналитика/<YYYY-MM-DD>.md` — снимок на день (подписчики, что известно по реакциям), не xlsx (чтобы не тащить `openpyxl`-зависимость туда, где это не нужно).
- Та же честная заглушка при отсутствии токена, что и у агента 6. Зависит от агента 6 (нет постов — нечего анализировать).

## Полный список проблем (для выбора решения)

1. **Push в отдельную ветку, не в `main`.** Обнаружено на агенте 1: CCR-рутина коммитит в свою ветку (`claude/wizardly-carson-*`), не в `main`, PR не открывает. Без ручного вмешательства каждый день — дублирование работы (агент не видит вчерашние необслитые изменения). Затронет ЛЮБОЙ агент, который пишет в репозиторий: 1, 2, 3, 4, 7 (аналитика), потенциально 5/6, если они тоже код в облачной рутине, а не локальный код.
2. **Финальный список тем публикаций не зафиксирован** (возрастные периоды решены 2026-09-18, см. раздел «Агент 2» выше — эта часть проблемы снята) — категоризация и подборка работают по рабочей черновой схеме тем, которую придётся пересматривать.
3. **Очерёдность агентов внутри суток:** 1→2→3→4 логически последовательны (каждый ждёт выход предыдущего), но заведены (или будут заведены) как независимые рутины по расписанию — нужно либо жёстко развести время запуска с запасом, либо явно передавать управление (агент N сам вызывает/триггерит агента N+1).
4. **Нет модели хранения статей вообще** — `data/статьи/` описана здесь как предложение, не согласована и не создана.
5. **Агент 5 (картинка):** неясно, годится ли уже подключённый коннектор `visualize` для генерации иллюстраций, или нужен отдельный код + отдельный API/ключ (DALL·E, Stability и т.п., с отдельной стоимостью).
6. **Агент 6 (публикация):** Telegram-канал и бот физически не существуют — нужно создать канал, завести бота через @BotFather, выдать токен, добавить бота админом.
7. **Агент 7 (аналитика):** Bot API не отдаёт просмотры постов канала — нужно решить, довольствоваться ли ограниченными метриками (подписчики, реакции) или искать обходной путь (например, ручная выгрузка статистики Telegram самим пользователем на вход агенту).
8. **Хранение секретов для облачных рутин** (Telegram-токен, возможные API-ключи для картинок) — CCR-рутины запускаются в облаке Anthropic, а не на сервере bayesum; нужно решить, где токены живут (переменные окружения рутины, отдельный секрет-хранитель) и кто их туда кладёт.
9. **SVG из агента 5 нельзя напрямую отправить в Telegram** (`sendPhoto` требует растр) — либо агент 5 переделывается на растровый вывод, либо агент 6 учится конвертировать SVG→PNG (нужен рендерер, которого может не быть в облачной песочнице), либо временно публикуем без картинки (текстом).
10. **`WebFetch` заблокирован сетевой политикой облачной песочницы для ВСЕХ проверенных внешних доменов** (обнаружено на первом реальном прогоне агента 2, 2026-09-17: `psyjournals.ru`, `antiage-expert.com`, `seniorgroup.ru`, `britannica.com`, `ncbi.nlm.nih.gov`, `who.int`, `wikipedia.org` — все вернули `EGRESS_BLOCKED`). Работает только `WebSearch` (идёт через инфраструктуру Anthropic, не напрямую). Агент 2 сам адаптировался — использовал `WebSearch` с `allowed_domains` вместо прямого чтения страниц, но это значит, что конспекты статей пишутся по сниппетам поиска, а не по полному тексту — это касается ЛЮБОГО будущего агента, которому нужен прямой доступ к странице (не только агента 2). Решение: либо просить администратора открыть эти домены в egress-политике окружения, либо принять `WebSearch`-only как постоянный режим работы.
11. **Прямой сетевой вызов (`curl`/Bash) к `api.telegram.org` тоже заблокирован политикой песочницы** (обнаружено на первом реальном прогоне агента 6, 2026-09-17: `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHANNEL_ID` заданы корректно, но egress-прокси окружения отвечает `403` на `CONNECT api.telegram.org:443` — «policy denial», см. `curl http://127.0.0.1:39993/__agentproxy/status`). Это отдельная проблема от отсутствия токена (раздел «Агент 6» выше) и от блокировки `WebFetch` (проблема №10) — токены есть, но сама песочница не пускает наружу даже через `Bash`/`curl`. Пока домен `api.telegram.org` не добавлен в egress-allowlist окружения агентов 6 и 7, обе рутины не могут ничего опубликовать/собрать по метрикам независимо от готовности канала и бота — публикация всех 5 одобренных на 2026-09-17 статей не состоялась именно по этой причине, статусы не менялись.
12. ~~Статус `selected` больше никто не проставляет~~ — **решено 2026-09-19**: агент 4 переведён на вход `sorted` (см. раздел «Агент 4» выше), читает рекурсивно по `data/статьи/**`. Отдельный шаг «отобрать 5 на публикацию» в конвейере по-прежнему отсутствует — сейчас агент 4 проверяет на непротиворечивость и одобряет/отклоняет ВСЕ статьи со статусом `sorted`, без ограничения по количеству в день; если нужен явный лимит публикаций в сутки, его придётся вводить отдельно (в агенте 4, 6 или новым агентом).
13. **НОВЫЙ, обнаружено 2026-09-19: две независимые живые рутины конкурируют за одни и те же `collected`-статьи в корне `data/статьи/`, и обе ведут в тупик.** Агент 3 (раскладка по папкам) и связка агент40/50/60/70/80 (рецензия) → агент41/51/61/71/81 (редактура) — оба берут файлы `data/статьи/*.md` со `status: collected` из корня, независимо друг от друга, по своему cron (агент 3 в 9:00 UTC; рецензенты тоже в 9:00, редакторы в 9:30 UTC — см. `docs/agent-prompts/README.md`). Если статью первым заберёт агент 3 — он переносит её в `data/статьи/<пол>/<возраст>/<тема>/` со `status: sorted`; рецензенты её больше не увидят (их поиск тоже нерекурсивный, только корень) — статья не получит возрастного ревью. Если статью первой заберёт связка рецензент→редактор — редактор правит текст на месте (файл остаётся в корне, НЕ переносится по папкам) и ставит `status: selected`; агент 3 её больше не тронет (ждёт `collected`), а агент 4 теперь ждёт `sorted`, а не `selected` (см. проблему №12) — статья застревает в `selected` навсегда, точно как 6 статей, вручную возвращённых в `collected` 2026-09-19 (см. историю коммитов). Это не единичный инцидент, а системный конфликт двух параллельных путей в конвейере — до его решения владельцем каждый день будут появляться новые осиротевшие статьи. Возможные направления решения (не выбраны): (а) разводить статьи по возрасту между двумя путями явно (например, только определённые age_range идут к рецензентам, остальные — сразу к агенту 3); (б) объединить путь — либо агент 3 тоже переносит `selected`-статьи в папки и агент 4 читает оба статуса, либо редакторы 41/51/61/71/81 сами переносят файл по папкам и ставят `sorted` вместо `selected`; (в) развести по времени и статусам так, чтобы один путь явно исключал другой (например, агент 3 пропускает статьи, для которых уже есть файл `data/рецензии/<id>-*.md`).

Все агенты 1–7 теперь заведены как облачные рутины (агенты 1–4 работают на реальных данных репозитория; 5 генерирует SVG уже сейчас; 6–7 разворачивают публикацию/аналитику, но упираются в отсутствующий Telegram-токен и корректно останавливаются с честным «не настроено» вместо попытки вслепую). Ничего из раздела «Проблемы» не решается автоматически этим документом — фиксирую полный список, чтобы решения принимались сразу по всей картине, а не патчами по одной проблеме за раз, как договорились.
