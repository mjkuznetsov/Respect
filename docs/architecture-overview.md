# Respect — обзор архитектуры (единая точка входа для ИИ и людей)

Дата: 2026-09-19. Назначение этого файла: если с текущей сессией ассистента (Claude) или с доступом к облачной инфраструктуре Anthropic что-то случится, **любой ИИ-агент или человек должен суметь восстановить полную картину системы, прочитав только этот репозиторий**, начиная с этого файла. Это не замена остальным `docs/*`, а карта — что где лежит и как связано, плюс самое актуальное состояние на дату выше.

**Правило поддержания этого файла и всей папки `docs/`:** любое изменение архитектуры, роли агента или промпта должно быть закоммичено и запушено в этот репозиторий в тот же день — минимум раз в сутки синхронизация накопленных изменений. GitHub — единственное постоянное хранилище состояния конвейера; сами облачные рутины (см. ниже) ничего не помнят между запусками, и текущая сессия ассистента может прерваться в любой момент.

## Что такое Respect

AI-платформа для людей 40+ (не 55+ — граница пересмотрена, см. «Известные несостыковки» ниже). Первый практический шаг к продукту — не сам продукт, а **контентный конвейер для Telegram-канала**: параллельно (1) собирать аудиторию через полезный контент и (2) копить данные о том, что этой аудитории откликается. Продуктовый контекст и мотивация — `docs/insights.md`.

## Как устроены «агенты» — важно понять это первым

Каждый агент 1–7 (и рецензенты/редакторы, см. ниже) — это не часть одного долгоживущего чата с ассистентом, а **отдельная облачная рутина (CCR, Claude Code cloud routine)**: запись в системе `RemoteTrigger` с cron-расписанием (в UTC), которая по расписанию поднимает **новую, изолированную сессию Claude без памяти о предыдущих запусках**, дающую ей репозиторий (git clone), доступные инструменты и, для агентов 6/7, доступ к секретам через привязанное облачное окружение (`environment`). Сессия делает свою работу, коммитит в свою ветку, пушит и исчезает.

Следствия:
- **Единственная память конвейера — этот git-репозиторий.** Агент N+1 узнаёт, что сделал агент N, только читая файлы, которые агент N закоммитил и запушил (и которые слил в `main` человек).
- **Промпт каждого агента — это то, что реально выполняется тем самым `RemoteTrigger`, а не текст в этом файле сам по себе.** Тексты синхронизированы (см. ниже, `docs/agent-prompts/`), но источник правды при расхождении — сама рутина (`RemoteTrigger action:"get"`, нужен доступ к API Anthropic, которого у стороннего ИИ, читающего только этот репозиторий, может не быть).
- Никакой отдельной БД/сервера нет сознательно — файлы + git вместо этого (человекочитаемо, диффится, не требует инфраструктуры).

## Пайплайн верхнего уровня

```
1 (источники) → 2 (сбор+категоризация) → 3 (раскладка по папкам) → 4 (проверка непротиворечивости) → 5 (картинка) → 6 (публикация Telegram) → 7 (аналитика)
```

Параллельно существует **вторая, отдельно заведённая цепочка** для возрастной адаптации текста, которая тоже претендует на статьи между агентом 2 и агентом 4 — см. секцию «Критическая нестыковка» ниже, это не решено.

### Таблица агентов 1–7

| № | Роль | trigger_id | Расписание (UTC) | Окружение | Вход (status) | Выход (status) | Полный промпт |
|---|---|---|---|---|---|---|---|
| 1 | Поиск источников | `trig_01JZ8oYGe7ceKnuwFBCoEgqu` | 6:00 | `env_01BDyCAoWgHK2nfGkb2Fg6hq` (Default) | — (веб-поиск) | новые строки в `data/источники.xlsx`, «На рассмотрении» | `docs/agent-prompts/agent-1.md` |
| 2 | Сбор и категоризация | `trig_01DiXUeBnYjSoqjzurJofvhu` | 8:00 | Default | строки xlsx с «Подтверждение = Да» | новые файлы `data/статьи/*.md`, `status: collected` | `docs/agent-prompts/agent-2.md` |
| 3 | Раскладка по хранилищу | `trig_01C8nQkEDQYTQxCPq9Pp5LcA` | 9:00 | Default | `collected` (только корень `data/статьи/`) | `sorted`, файл перенесён в `data/статьи/<пол>/<возраст>/<тема>/` | `docs/agent-prompts/agent-3.md` |
| 4 | Проверка непротиворечивости | `trig_012HBqSFJ9McQrnUQjZEvcHj` | 10:00 | Default | `sorted` (рекурсивно) | `approved` / `rejected` (+`rejection_reason`) | `docs/agent-prompts/agent-4.md` |
| 5 | Генерация изображения | `trig_01X7nLjfT6qcEAWZ9cjxhyi1` | 11:00 | Default | `approved` без `image_path` | добавляет `image_path` → `data/статьи/images/<id>.svg` | `docs/agent-prompts/agent-5.md` |
| 6 | Публикация в Telegram | `trig_01U8ZbedfqqYVS6KGvfhqzny` | 12:00 | `env_01KpNj3TpysMby4SW3VVDsMT` (respect-telegram, секреты) | `approved` | `published` (+`published_at`, `telegram_message_id`) | `docs/agent-prompts/agent-6.md` |
| 7 | Аналитика | `trig_01Khmi3sPLDTCXiKdT8VJ3Sn` | 13:00 | respect-telegram | `published` + `telegram_message_id` | `data/аналитика/<дата>.md` | `docs/agent-prompts/agent-7.md` |

**Известный технический долг:** агенты 5, 6, 7 ищут кандидатов нерекурсивным `data/статьи/*.md` (только корень) — начиная с 2026-09-19 статьи после агента 3 физически лежат в подпапках, так что эти три агента их не найдут, пока их прогдром не поправят на `data/статьи/**/*.md` (агенты 2 и 4 уже рекурсивные). Зафиксировано как открытая проблема прямо в файлах `agent-5.md`/`agent-6.md`/`agent-7.md`.

### Вторая цепочка: возрастная рецензия/редактура (агенты 40–81)

Заведена **2026-09-18** как альтернатива/дополнение агенту 3 в его старой роли «отбора». Пять возрастных когорт, каждая — пара «рецензент» (пишет замечания от лица персоны своего поколения) → «редактор» (правит статью с учётом замечаний, ставит `status: selected`):

| Когорта | Рецензент (trigger) | Редактор (trigger) | Расписание |
|---|---|---|---|
| 40-49 | агент40 `trig_018Ldt5veEq4B4AdD52AVYcb` | агент41 `trig_011mKmCQZmRgAY7oMsKVWPnw` | 9:00 / 9:30 UTC |
| 50-59 | агент50 `trig_01E59uksjSYwJcqpJgxf9cSp` | агент51 `trig_01GcVyKR8kSCyCJ1mu7ZLswo` | 9:00 / 9:30 |
| 60-69 | агент60 `trig_01UtdAzKJE2518XtnhBUJ7qv` | агент61 `trig_01NnVUhrW2y8hxby2NUMwgxL` | 9:00 / 9:30 |
| 70-79 | агент70 `trig_01HKacrPGKiMg5WdX64ptXEg` | агент71 `trig_014GL6cesRE9XJZ6JEx9vpzW` | 9:00 / 9:30 |
| 80+ | агент80 `trig_017fAc5uPPaRh32AEweaPnkZ` | агент81 `trig_01ECPp36Hv1xQvjekwUygUgm` | 9:00 / 9:30 |

Промпты — `docs/agent-prompts/reviewer-<N>.md` и `docs/agent-prompts/editor-<N>.md`, индекс и подробная схема данных — `docs/agent-prompts/README.md`. Рецензент пишет вердикт в `data/рецензии/<id>-<когорта>.md`, не трогая саму статью; редактор читает замечания, правит статью **на месте** (в корне `data/статьи/`, без переноса по папкам) и ставит `status: selected`.

## 🔴 Критическая нестыковка (не решена, актуальна на 2026-09-19)

Агент 3 и связка рецензент→редактор — **два независимых механизма, конкурирующих за одни и те же `data/статьи/*.md` со `status: collected` в корне**, оба запускаются около 9:00 UTC. Какой из них первым заберёт файл — то и происходит:
- заберёт агент 3 → файл уйдёт в подпапку со `status: sorted`, рецензенты (нерекурсивный поиск) его больше не увидят;
- заберёт рецензент/редактор → файл останется в корне со `status: selected`, но агент 3 ждёт только `collected`, а агент 4 (с 2026-09-19) ждёт только `sorted` — статья зависает в `selected` навсегда.

Ровно это произошло с 6 статьями, которых пришлось вручную вернуть в `collected` 2026-09-19 (см. `git log` за эту дату). Без решения владельца эта ситуация будет повторяться каждый день. Варианты решения намечены, но не выбраны — см. `docs/agents-design.md`, проблема №13 (последняя в списке).

## Модель данных

### `data/статьи/<id>.md` — одна статья

Frontmatter (YAML) + тело (конспект/текст на русском). Ключевые поля:

```yaml
id: <slug, = имя файла без .md>
source_url: <URL первоисточника>
source_name: <название источника>
title: "<заголовок>"
gender: мужчины | женщины | все
age_range: 40-49 | 50-59 | 60-69 | 70-79 | 80+ | все
topic: здоровье | долголетие | психология | спорт | досуг | работа | финансы | технологии | юридическая защита | обучение
status: collected → sorted → approved/rejected → published
collected_date / sorted_date / selected_date / edited_date / checked_date / published_at: <даты>
editor / editor_notes: <если прошла через возрастную редактуру>
rejection_reason: <если rejected>
image_path: data/статьи/images/<id>.svg
telegram_message_id: <после публикации>
```

**Расположение на диске меняется по ходу конвейера:**
- `collected` (после агента 2) — всегда в корне `data/статьи/<id>.md`.
- `sorted` (после агента 3) — перенесена в `data/статьи/<пол>/<возраст>/<тема>/<id>.md`.
- `selected` (после связки рецензент→редактор) — **остаётся в корне**, путь не меняется (см. критическую нестыковку выше).
- `approved`/`rejected`/`published` — путь не меняется относительно того, где статья была на момент вердикта агента 4 (то есть физически может быть и в подпапке, и в корне — зависит от того, каким путём статья туда попала).

### Другие данные
- `data/источники.xlsx` — источники (агент 1 пишет, агент 2 только читает, колонку «Подтверждение» проставляет человек).
- `data/queries_log.md` — журнал поисковых запросов агента 1 (дедуп формулировок).
- `data/рецензии/<id>-<когорта>.md` — вердикты возрастных рецензентов.
- `data/подборки/<дата>.md` — **устарело**: создавал старый агент 3 (роль отбора 5 статей), новый агент 3 этот файл не создаёт.
- `data/аналитика/<дата>.md` — ежедневный снимок метрик от агента 7.
- `data/статьи/images/<id>.svg` — иллюстрации от агента 5.

## Индекс `docs/`

| Файл | Что внутри |
|---|---|
| `docs/insights.md` | Продуктовый контекст и мотивация проекта Respect |
| `docs/agent-pipeline.md` | Таблица агентов 1–7 верхнего уровня, кто в контуре человека, история решений по возрастной сетке |
| `docs/agents-design.md` | Детальная спецификация каждого агента (вход/выход/механизм) + **пронумерованный список открытых проблем в конце файла** — начинать поиск известных проблем нужно оттуда |
| `docs/audience-behavior-rules.md` | Обязательные поведенческие guardrail'ы (тон, что нельзя говорить аудитории 40+, п.5 — возрастная сетка) |
| `docs/content-calendar-40-99.md` | Черновой календарь тем — вспомогательная подсказка агенту 2, не источник правды по темам |
| `docs/agent-prompts/README.md` | Индекс промптов агентов 1–7 + рецензентов/редакторов 40–81 + черновика «Летописца» |
| `docs/agent-prompts/agent-1.md` … `agent-7.md` | Полный текст промпта каждого из семи основных агентов |
| `docs/agent-prompts/reviewer-*.md`, `editor-*.md` | Полный текст промптов возрастных рецензентов/редакторов |
| `docs/agent-prompts/writer.md` | Черновик «Летописца» (агент 8) — не развёрнут, не используется в текущей цепочке |
| `docs/architecture-overview.md` | Этот файл |

## Известные несостыковки в самих docs (не исправляются автоматически)

- `docs/agent-pipeline.md` (интро, строка 3) всё ещё называет аудиторию «людей 55+» — фактическая граница везде в промптах и в `audience-behavior-rules.md` уже «40+». Оставлено намеренно по прошлым инструкциям владельца («докс не трогать без явной просьбы»).
- Возрастная сетка **зафиксирована 2026-09-18**: `40-49 / 50-59 / 60-69 / 70-79 / 80+` (заменила недолго прожившую `55-65/65-75/75-85/85+` от 2026-09-17).
- Финальный список тем публикаций (`topic`) — рабочий, не утверждён окончательно (см. `docs/agents-design.md`, проблема №2, частично снята).

## Полный список открытых проблем

Единый источник правды — **нумерованный список в конце `docs/agents-design.md`** (сейчас пункты 1–13). Не дублирую здесь, чтобы не разъезжались две копии — при обновлении смотреть и править только там. Самые актуальные на 2026-09-19: №9 (SVG→растр для Telegram), №11 (egress-блокировка `api.telegram.org` для агентов 6/7 — публикация физически невозможна, пока не снята), №13 (критическая нестыковка агента 3 и возрастной редактуры, см. выше).

## Инфраструктура и секреты

- Git: `https://github.com/mjkuznetsov/Respect`, ветка по умолчанию `main`. Каждая рутина работает в своей ветке (`agent-N/<дата>` для 1–4, `claude/<...>` для остальных) и пушит только туда — слияние в `main` делает владелец (или ассистент по явной просьбе), это единственная точка ручного контроля в конвейере.
- Два облачных окружения (`environment`) в Anthropic CCR:
  - **Default** (`env_01BDyCAoWgHK2nfGkb2Fg6hq`) — агенты 1–4, рецензенты/редакторы. Без секретов.
  - **respect-telegram** (`env_01KpNj3TpysMby4SW3VVDsMT`) — агенты 6 и 7 отдельно, потому что в общем «Default»-окружении переменные окружения видны всем рутинам этого окружения, а `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHANNEL_ID` не должны быть доступны агентам 1–5.
- Telegram-канал: `@respect_age`. Публикация текстом (`sendMessage`), не фото — картинки пока SVG, Telegram их не принимает (проблема №9).
- MCP-коннекторы, используемые агентами: `Claude_Docs`/`Claude-Docs` (доступ к докам), `Claude_Code_Remote`, `Firecrawl` (только агент 2, обход egress-блокировки WebFetch), `visualize` (только агент 1, не задействован по назначению).

## Если ты — другой ИИ и читаешь это впервые

1. Не доверяй промптам агентов 1–7/40–81 в этом репозитории вслепую как «истине в последней инстанции» — это snapshot на дату вверху файла; если нужен гарантированно актуальный текст, запрашивай саму рутину через `RemoteTrigger action:"get"` (если есть доступ к API).
2. Прежде чем менять роль/промпт любого агента — прочитай пронумерованный список проблем в конце `docs/agents-design.md`, особенно №13: два механизма конкурируют за один и тот же пул статей, и это легко случайно усугубить.
3. После любого изменения архитектуры/промпта: обнови соответствующий файл в `docs/agent-prompts/`, обнови `RemoteTrigger` (`action:"update"`) на реальной рутине (не только файл в репозитории — иначе они разойдутся), закоммить и запушить в `main` в тот же день.
4. Никогда не клади секреты (Telegram-токен и т.п.) в файлы, коммиты или ответы — они живут только в переменных окружения `environment` `respect-telegram`.
